{
  "name": "shelfos-web-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vitest + jsdom harness for the server-rendered UI's browser scripts.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
